/**
 * @deprecated Use `profiles.zoho_connected` via `src/db/zohoConnected.ts`.
 * Kept for backward-compatible imports only.
 */

export {
  isProfileZohoConnected,
  assertApplywizzZohoConnected,
  fetchZohoConnectedApplywizzIdSet,
} from '../db/zohoConnected.js';
